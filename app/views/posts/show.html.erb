

<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<% if current_user %>
    <%= link_to 'Edit', edit_post_path %>
    <%= link_to 'Delete', post_path(@post), method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>